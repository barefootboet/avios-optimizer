// Tailwind v4: theme, plugins, and content scanning are configured in globals.css via
// @theme, @plugin, and automatic content detection. This file is kept minimal.
import type { Config } from "tailwindcss";

const config: Config = {};

export default config;
